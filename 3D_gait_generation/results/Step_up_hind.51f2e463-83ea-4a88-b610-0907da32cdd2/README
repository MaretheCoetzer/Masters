Step-up-hind-legs, eps=5, stair_height=3cm, x starting position removed, random seed added, x final distance constraints removed to see if it then solves feasibly, cost function altered to include body starting position, max initial hind feet position added